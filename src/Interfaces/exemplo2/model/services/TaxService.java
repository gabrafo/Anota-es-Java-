package Interfaces.exemplo2.model.services;

public interface TaxService {
    double tax (double amount); // Define que toda classe que implementar essa interface terá que definir também esse método
}
